import type { Project } from '@/hooks/useProjects'

/** Stub — implemented in plan 02-07 (Jira toggle, Slack display shell, health badge, stub check-health). */
export function IntegrationsTab({ project: _project }: { project: Project }) {
  return <p className="text-sm text-slate-500">Integration config coming in plan 02-07.</p>
}
