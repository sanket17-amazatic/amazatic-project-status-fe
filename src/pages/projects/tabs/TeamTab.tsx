import type { Project } from '@/hooks/useProjects'

/** Stub — implemented in plan 02-07 (PM reassign + add/remove members via useUsers()). */
export function TeamTab({ project: _project }: { project: Project }) {
  return <p className="text-sm text-slate-500">Team management coming in plan 02-07.</p>
}
