/**
 * Placeholder root. Routing, the auth guard, and the sidebar/top-bar shell
 * land in the auth-shell plan (02-04, D-14/D-16) — this plan only stands up
 * the data layer (api.ts, queryClient.ts, authStore.ts) that shell consumes.
 */
export default function App() {
  return null
}
