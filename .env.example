# Amazatic Project Status — frontend environment (local dev template).
# Copy to .env and adjust if needed. NEVER commit real values.

# Backend API base URL. Dev SPA runs on :5173 (already in the backend's
# CORS_ALLOWED_ORIGINS / CSRF_TRUSTED_ORIGINS local .env allowlist).
VITE_API_BASE_URL=http://localhost:8000
